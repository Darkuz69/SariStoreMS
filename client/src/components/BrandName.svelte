<h1 class=" text-4xl font-extrabold text-[var(--primary)]">SariStore<span class="text-[var(--secondary)]">MS</span></h1>

<style>
:root {
    --primary-muted: hsl(214 78% 50%);
    --secondary-muted: hsl(35 58% 30%);
}

h1 {
    text-shadow: 
    0px 1px var(--primary-muted),
    0px 2px var(--primary-muted),
    0px 3px var(--primary-muted),
    0px 4px var(--primary-muted),
    0px 5px var(--primary-muted);
}

span {
    text-shadow: 
    0px 1px var(--secondary-muted),
    0px 2px var(--secondary-muted),
    0px 3px var(--secondary-muted),
    0px 4px var(--secondary-muted),
    0px 5px var(--secondary-muted);
}
</style>